autogenerated = 0 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
#monitor=eDP-1,1920x1080@120,0x0,0.75
#monitor=eDP-1,2560x1440@120,0x0,1,vrr,1

monitor=eDP-1,Modeline 164.81 1640 1688 1720 1800 720 723 750 763 -333HSync -VSync,0x0,1

#monitor=eDP-1,Modeline 285 1920 2028 2076 2076 1080 1090 1100 1142 -HSync -VSync,0x0,1
#monitor=eDP-1,1920x1080@120,0x0,1,vrr,1
#monitor=eDP-1,Modeline "1640x720_119.81" 207.25 1640 1760 1936 2232 720 723 733 775 -HSync +VSync,0x0,1
#monitor=eDP-2,1920x1080@120,0x0,1,vrr,1
#monitor=eDP-1,1920x1080@120,0x0,1
#monitor=eDP-2,1920x1080@120,0x0,1

# DP-1
monitor=DP-1,1920x1080@60,0x-1080,1


# =============
# Mirror Display
# =============
# Uncomment if you want use this
# This mirror Laptop Screen to HDMI

#monitor=eDP-1,1920x1080@120,0x0,1,mirror,HDMI-A-1
#monitor=,1920x1080@60,auto,1,mirror,eDP-1

# Best preset for Projector Screen
#monitor=eDP-1,1280x720@120,0x0,1,mirror,HDMI-A-1
#monitor=,1280x720@60,auto,1,mirror,eDP-1

# ====================================


# ============
# HDMI
# ============
# Uncomment on position Screen HDMI on you like

#On Top Laptop Screen
monitor=HDMI-A-1,1920x1080@60,0x-1080,1

## Best Preset for Peojector Screen on Top Laptop Screen
#monitor=HDMI-A-1,1280x720@60,0x-1080,1

#On Bottom Laptop Screen
#monitor=HDMI-A-1,1920x1080@60,0x1080,1

## Best Preset for Projector Screen on Bottom Laptop Screen
#monitor=HDMI-A-1,1280x720@60,0x1080,1


#On Right Laptop Screen
#monitor=HDMI-A-1,1920x1080@60,1920x0,1

## Best Preset for Projector Screen on Right Left Laptop Screen
#monitor=HDMI-A-1,1280x720@60,1920x0,1


#On left Laptop Screen
#monitor=HDMI-A-1,1920x1080@60,-1920x0,1

## Best Preset for Projector Screen on Left Laptop Screen
#monitor=HDMI-A-1,1280x720@60,-1920x0,1


#====================================


$w1 = hyprctl hyprpaper wallpaper "eDP-2,~/Pictures/tMrvygQ.jpg"


misc {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    force_default_wallpaper = -1 # Set to 0 to disable the anime mascot wallpapers
}

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
exec-once = qpwgraph & blueman-applet & hyprpaper & swaync & waybar & fusuma | tee $HOME/.cache/fusuma/fusuma.log
# exec-once = /usr/lib/policykit-1-gnome/polkit-gnome-authentication-agent-1
exec-once = /usr/lib/mate-polkit/polkit-mate-authentication-agent-1
#exec-once = /usr/lib/polkit-kde-authentication-agent-1
#exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=hyprland
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = nm-applet --indicator
exec-once = /home/kayagi/mango/startmango.sh
#exec-once = kitty -c "~/.config/hypr/kittybg.conf" --class="kitty-bg" "/home/kayagi/.config/hypr/cava.sh"
#exec-once = $HOME/.config/hypr/config_custom/nm-tray.sh

# Idle Monitor
#exec-once = swayidle -w timeout 120 'hyprctl dispatch dpms off' resume 'hyprctl dispatch dpms on'
exec-once = hypridle

# Xwayland
#exec-once=xprop -root -f _XWAYLAND_GLOBAL_OUTPUT_SCALE 32c -set _XWAYLAND_GLOBAL_OUTPUT_SCALE 2
#env = GDK_SCALE=2
#env = XCURSOR_SIZE=16


# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf



# Set programs that you use
$terminal = kitty
$fileManager = dolphin
$menu = wofi --show drun

# Some default env vars.
env = HYPRCURSOR_THEME,volantes_light_cursors
env = HYPRCURSOR_SIZE,16
#env = XCURSOR_SIZE,16
#env = QT_STYLE_OVERRIDE,gtk2
env = QT_QPA_PLATFORMTHEME,qt5ct # change to qt6ct if you have that
env = WLR_NO_HARDWARE_CURSORS,1
#env = WLR_DRM_DEVICES,/dev/dri/card1
# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =
    numlock_by_default = true
    #force_no_accel = true
    

    follow_mouse = 1

    touchpad {
        natural_scroll = true
        scroll_factor = 0.5
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 10
    border_size = 2
    col.active_border = rgba(60C0CAee) rgba(597E6Fee) 45deg
    col.inactive_border = rgba(595959aa)

    #no_cursor_warps = true

    layout = dwindle

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 10

    blur {
        enabled = true
        size = 3
        passes = 1
        
        vibrancy = 0.1696
    }

    layerrule = blur,wofi
    layerrule = blur,launcher
    #layerrule = blur,waybar
    layerrule = blur,calender


    layerrule = blur, swaync-control-center
    layerrule = blur, swaync-notification-window

    layerrule = ignorezero, swaync-control-center
    layerrule = ignorezero, swaync-notification-window



    drop_shadow = yes
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = true

    # Some default animations, see https://wiki.hyprland.org/Co1nfiguring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    bezier = por, 0.5, 1.2, 0.5, 1
    bezier = te, 0.4, 1.2, 0.01, 0.98

    animation = windows, 1, 7, por
    animation = windowsOut, 1, 7, te, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, te
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = off
    workspace_swipe_distance = 2000
    workspace_swipe_min_speed_to_force = 10000
    workspace_swipe_cancel_ratio = 0.15
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device{
    name = epic-mouse-v1
    sensitivity = -0.8
}

device {
    name = a4tech-usb-device
    #sensitivity = -0.1
    sensitivity = -0.7
    #accel_profile = flat
}



plugin {
    hyprwinwrap {
        # class is an EXACT match and NOT a regex!
        class = kitty-bg
    }
}





windowrulev2 = float,class:^(kitty-bg)$
windowrulev2 = noblur,class:^(kitty-bg)$
windowrule = size 10 40,^(kitty-bg)$




# Example windowrule v1
# windowrule = fullscreen, ^(kitty)$
windowrule = noblur,class:^(google-chrome)$ # disables blur for firefox
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
windowrulev2 = float,class:^(org.kde.polkit-kde-authentication-agent-1)$

#windowrulev2 = nomaximizerequest, class:.* # You'll probably like this.
windowrulev2 = stayfocused, title:^()$,class:^(steam)$
windowrulev2 = minsize 1 1, title:^()$,class:^(steam)$

windowrulev2 = workspace 10 , class:^(org.rncbc.qpwgraph)$
windowrulev2 = workspace 6 , class:^(Waydroid)$
windowrulev2 = fullscreen , class:^(Waydroid)$
windowrulev2 = fullscreen , class:^(Vita3K)$
windowrulev2 = idleinhibit fullscreen, class:^(firefox)$
windowrulev2 = idleinhibit fullscreen, class:^(zoom)$



#windowrule = move 1380 40,^(nm-tray)$
#windowrulev2 = float,class:^(nm-tray)$

# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Return, exec, kitty
bind = $mainMod, T, exec, kitty
bind = $mainMod, W, killactive, 
bind = $mainMod, M, exit, 
bind = $mainMod SHIFT, E, exec, dolphin
bind = $mainMod, V, togglefloating, 
bind = $mainMod ALT, V, fullscreen, 
bind = $mainMod, D, exec, tofi-run | xargs hyprctl dispatch exec --
bind = $mainMod, N, pseudo, # dwindle
bind = $mainMod, B, togglesplit, # dwindle
bind = $mainMod SHIFT, D, exec, wofi --show drun

# Monitor Off
bind = $mainMod, L, exec, hyprctl dispatch dpms off && hyprlock
misc {
	mouse_move_enables_dpms = true #prevents accidental wake up
	key_press_enables_dpms = false
}

# Example volume button that allows press and hold, volume limited to 150%
binde=, XF86AudioRaiseVolume, exec, wpctl set-volume -l 1.5 @DEFAULT_AUDIO_SINK@ 2%+
#binde=, XF86AudioRaiseVolume, exec, volumectl -u up

# Example volume button that will activate even while an input inhibitor is active
binde=, XF86AudioLowerVolume, exec, wpctl set-volume -l 1.5 @DEFAULT_AUDIO_SINK@ 2%-
#binde=, XF86AudioLowerVolume, exec, volumectl -u down

# Special Keys
#binde=, XF86AudioMute, exec, volumectl toggle-mute
##binde=, XF86AudioMicMute, exec, volumectl -m toggle-mute


# Screenshot a window
bind = $mainMod, S, exec, hyprshot -m window
# Screenshot a monitor
bind = , PRINT, exec, hyprshot -m output
# Screenshot a region
bind = $mainMod SHIFT, S, exec, hyprshot -m region

# Special Keys
bind = , xf86monbrightnessup, exec, brightnessctl set --device=amdgpu_bl0 2%+
bind = , xf86monbrightnessdown, exec, brightnessctl set --device=amdgpu_bl0 2%-
bind = , xf86audiomute, exec, wpctl set-mute @DEFAULT_SINK@ toggle

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, TAB, togglespecialworkspace, magic
bind = $mainMod SHIFT, TAB, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod ALT, x, movewindow

bindm = $mainMod, mouse:273, resizewindow
bindm = $mainMod ALT, z, resizewindow
